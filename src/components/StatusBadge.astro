---
interface Props {
  status: string;
}

const { status } = Astro.props;
---

<span class="inline-flex rounded border border-border bg-bg px-2 py-0.5 font-mono text-[0.65rem] uppercase tracking-wider text-text-muted">
  {status}
</span>
